Os seeders sao sementes com eles geramos dados para popular nosso banco e ter dados para teste

-terminal
php artisan make:seeder ContatosTableSeeder

-vamos criar uma repticao para criar contatos

vamos importar um helper chamado faker e nosso Model Contato

use App\Contato;
use Faker\Factory as Faker;


$faker = Faker::create();

for ($i=0; $i <30 ; $i++) {
    Contato::create([
      'nome'=>$faker->name,
      'telefone'=>$faker->numerify($string = '##########');
    ]);
}


em DatabaseSeeder criar uma chamada call para nosso arquivo de Seeder
// $this->call(ContatosTableSeeder::class);


pronto seed criado agora podemos executar
-terminal
php artisan db:seed

agora vamos ver no tinker os dados criados
php artisan tinker
App/Contato::all();
